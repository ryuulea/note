Public Sub DebugDeleteAll()

    Dim wsMail As Worksheet
    Dim lastMail As Long, i As Long
    Dim fnum As Integer, filePath As String
    Dim val As String
    
    Set wsMail = ThisWorkbook.Sheets("メール本文テンプレート情報")
    lastMail = wsMail.Cells(wsMail.Rows.Count, "A").End(xlUp).Row
    
    filePath = ThisWorkbook.Path & "\debug_delete_all.txt"
    fnum = FreeFile
    Open filePath For Output As #fnum
    
    For i = 11 To lastMail
        val = CStr(wsMail.Cells(i, 4).Value2)   ' D列
        Print #fnum, "行:" & i & " D列=[" & val & "]"
    Next i
    
    Close #fnum
    MsgBox "DebugDeleteAll 完了: " & filePath

End Sub
